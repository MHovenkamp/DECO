# ifndef SUPPORT_HPP
# define SUPPORT_HPP

#include <Arduino.h>
#include "mbed.h"
#include "rtos.h"
#define MS(x) std::chrono::milliseconds(x)
#define US(x) std::chrono::microseconds(x)


#endif //SUPPORT_HPP